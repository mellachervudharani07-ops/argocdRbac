apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: project-b-staging
  namespace: argocd
spec:
  generators:
    - list:
        elements:
          - app: app1
          - app: app2
  template:
    metadata:
      name: project-b-staging-{{app}}
    spec:
      project: project-b
      source:
        repoURL: https://github.com/mellachervudharani07-ops/argocdRbac
        targetRevision: master
        path: project-b/apps/{{app}}
      destination:
        server: https://kubernetes.default.svc
        namespace: staging
      syncPolicy:
        automated:
          prune: true
          selfHeal: true